# drm.spec
#
%define name            drm
%define version         @VERSION@
%define release         1
 
Summary:	Digital Radio Mondiale (DRM) software receiver.
Name:		%{name}
Version:	%{version}
Release:	%{release}
License:	GPL
Group:		Applications/Communications
Source0:	%{name}-%{version}.tar.gz
URL:		http://drm.sourceforge.net/
BuildRoot:	%{_tmppath}/%{name}-%{version}-%{release}-root
BuildRequires:	qmake
BuildRequires:	fftw3 fftw3-devel
BuildRequires:	libqt4, libqt4-devel
BuildRequires:	qwt6, qwt6-devel
BuildRequires:	libgps20, gpsd-devel
BuildRequires:	libsndfile1, libsndfile-devel
BuildRequires:	libpcap1, libpcap-devel
BuildRequires:	hamlib, hamlib-devel
BuildRequires:  libportaudio2, portaudio-devel

%description
Digital Radio Mondiale (DRM) is the digital radio standard for
the long, medium and short wave ranges.

Dream is an open source software implementation of a DRM receiver.

%prep
%setup -q

%build
%configure --with-journaline --with-hamlib
make

%install
rm -rf %{buildroot}
%makeinstall

%clean
rm -rf %{buildroot}

%files
%defattr(-, root, root)
%doc AUTHORS ChangeLog COPYING INSTALL NEWS README TODO
%{_bindir}/drm

%changelog
* Tue Oct 23 2012 Julian Cable <jcable@sf.net>
- Qt4 and others
* Wed Apr 24 2005 Alexander Kurpiers <kurpiers@sf.net>
- add libjournaline and hamlib
* Wed Dec 17 2003 Alexander Kurpiers <kurpiers@sf.net>
- some changes for RedHat
* Sun Nov 30 2003 Tomi Manninen <oh2bns@sral.fi>
- First try

